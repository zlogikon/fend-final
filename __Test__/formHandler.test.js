import { postData } from '../src/client/js/app';

describe("Testing the dates functionality", () => {

    test('this is getDate test', () => {
        const testDate = 'Positive'
        expect(postData).toBeDefined();
      }); 

})